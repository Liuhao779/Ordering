<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dcxt.front.mapper.FrontCheckTableMapper">
    <select id="getByTableinfo" parameterType="TableBean" resultType="TableBean">
        SELECT * FROM t_tableinfo <where>
        <!--<if test="state != null">-->
            <!--state = #{state}-->
        <!--</if>-->
        <!--<if test="uid != 0">-->
            <!--and uid = #{uid}-->
        <!--</if>-->
    </where>

    </select>


</mapper>
